// Barrel export — add one line per table as features are built.
// Every table exported here must also appear in @DriftDatabase(tables: [...])
// inside database.dart.

export '../../features/events/domain/event.dart';
// export '../../features/counter/domain/counter.dart';
// export '../../features/counter/domain/daily_count.dart';